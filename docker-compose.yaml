version: "3.9"
services:
  agent:
    build: .
    working_dir: /workspace/agent
    command: uv run python main.py
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - .:/workspace
